name: Crypto Behavior Dashboard

on:
  schedule:
    - cron: "10 16 * * *"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/daily-dashboard.yml"
      - "config.yaml"
      - "src/**"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  collect-and-build:
    runs-on: ubuntu-latest
    env:
      TZ: Asia/Shanghai

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Collect market data
        run: python3 -m src.cli daily-collect

      - name: Update event outcomes
        run: python3 -m src.cli update-outcomes --all

      - name: Generate Markdown report
        run: python3 -m src.cli generate-report --date "$(date +%F)"

      - name: Generate behavior archive
        run: python3 -m src.cli generate-behavior

      - name: Generate static dashboard
        run: python3 -m src.cli generate-dashboard

      - name: Export static JSON data
        run: python3 -m src.cli export-json

      - name: Commit updated dashboard and history data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/processed/ docs/data/ docs/reports/ docs/index.html events/events.jsonl reports/*.md
          git diff --cached --quiet || git commit -m "Update daily crypto behavior dashboard"
          git push
